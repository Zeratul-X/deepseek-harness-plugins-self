fix(file-ref): 恢复输入框上方的引用 dock(扫描草稿 @token 展示)，输入框保持纯文本

- 上一版重构误删了 dock，用户失去引用展示与选行入口，此版恢复：
  dock 不再依赖 chip/occurrences，改为扫描草稿中的 '@token' 展示(全路径 + ✕)
- 点击 dock 项：按当前草稿重新定位 token → 弹代码预览选行(点行号/Shift/拖拽/Ctrl+F)
- ✕：从草稿中删除对应 '@token'(连同一个相邻空格)
- 确认选行：原位替换 token 为纯文本 '@path line a-b'
- 输入框仍是纯文本，无 chip/占位符，光标正常；点击输入框里的 @path 文本同样可弹预览